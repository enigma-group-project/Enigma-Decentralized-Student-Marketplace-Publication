Foundry-based. Correctness via `forge test` per-slice suites; robustness via fuzz + invariant tests (`Invariant.t.sol`). Gas via `forge test --gas-report` (avg/call). Slither runs as a required CI job per PR. Deployed to local Anvil (31337) and public Sepolia (11155111), exercised end-to-end via a dual-network ethers.js client. Build/test/analysis re-run in CI — every result reproducible.
