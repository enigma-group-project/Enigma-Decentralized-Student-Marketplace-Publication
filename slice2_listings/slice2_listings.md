**Owner: Member 2.** `Marketplace` records listings: `createListing` stores `Listing{seller, price(ENGC), metadata, status}`; `getListing(id)`/`totalListings()` expose them; ids are monotonic; invalid metadata/price revert.